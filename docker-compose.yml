version: '3.9'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile  # Змініть цей рядок, якщо ваш Dockerfile має інше ім'я
    command: daphne fishyandex.asgi:application -b 0.0.0.0 -p 8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
      - selenium

  nginx:
    build:
      context: .
    ports:
      - "80:80"
    volumes:
      - ./nginx:/etc/nginx/conf.d
      - static_volume:/app/static
    depends_on:
      - web

  db:
    image: postgres:15.2
    environment:
      POSTGRES_DB: fishyandex
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pegasus
    volumes:
      - postgres_data:/var/lib/postgresql/data/

  redis:
    image: "redis:latest"

  selenium:
    image: selenium/standalone-chrome:latest
    build:
      context: .
    volumes:
      - /dev/shm:/dev/shm

volumes:
  postgres_data:
  static_volume:
